<%= form_for [commentable, Comment.new] do |f| %>
<div class="form-group">
	<%= f.text_area :body, class: "form-control", placeholder: "Ajouter un commentaire" %>
</div>
<%= f.submit "Commenter", class: "btn btn-outline-primary" %>
<%end%>